# Core
PyYAML>=6.0

# MT5 connector (Windows only). Install on your Windows laptop:
#   pip install MetaTrader5
# Left commented so `pip install -r requirements.txt` works on Linux/Mac too.
# MetaTrader5>=5.0.45

# Data / analysis
pandas>=2.0
numpy>=1.24

# Technical indicators (pure python, cross-platform)
pandas-ta>=0.3.14b

# Testing
pytest>=8.0
