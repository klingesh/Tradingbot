# Core
PyYAML>=6.0

# MT5 connector (Windows only - install when running the live/demo connector)
# MetaTrader5>=5.0.45

# Data / analysis
pandas>=2.0
numpy>=1.24

# Technical indicators (pure python, cross-platform)
pandas-ta>=0.3.14b

# Testing
pytest>=8.0
