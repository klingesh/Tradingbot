# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Cached market data (re-downloaded on demand)
data_cache/

# Live bot logs / journal (local runtime data)
logs/

# Personal live config (copy from config/live_config.example.yaml).
# Keeps your symbols/risk/dry_run settings from conflicting with git pulls.
config/live_config.yaml

# Publisher settings - contains a GitHub token. NEVER commit this.
config/publish.yaml

# Secrets / broker credentials - NEVER commit these
.env
*.secret
credentials.json
mt5_login.json

# OS / editor
.DS_Store
Thumbs.db
